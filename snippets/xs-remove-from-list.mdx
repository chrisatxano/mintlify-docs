---
title: XS -Remove From List
---

# <img src="../assets/docuBadge (11).png" alt="" data-size="line"> Remove From List <a href="#xs-remove-from-list" id="xs-remove-from-list"></a>

```javascript
redis.remove {
  key = ""
  value = ""
  count = 0
}
```

| Parameter | Purpose                         | Example                           |
| --------- | ------------------------------- | --------------------------------- |
| key       | List key                        | `"active_users"`, `"blocked_ips"` |
| value     | Value to remove                 | `"user123"`, `{id: 456}`          |
| count     | Number of occurrences to remove | `0` (all), `1`, `-2`              |

<details>

<summary>Example</summary>

```javascript
redis.remove {
  key = "active_sessions"
  value = $session.id
  count = 0
}
```

* Removes matching values from list
* Count=0 removes all occurrences
* Count>0 removes from head to tail
* Count<0 removes from tail to head

</details>

